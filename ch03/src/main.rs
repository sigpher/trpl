use ch03::data_types::{self, control_flow, functions::print_labeled_measurement};

fn main() {
    // data_types::compound_types();
    data_types::array();
    data_types::functions::func_one();
    print_labeled_measurement(10, 'm');
    control_flow();
}
